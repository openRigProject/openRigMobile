name: openrig_mobile
description: openRig Mobile — lightweight iOS/Android rig control and spot viewer.
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  openrig_core:
    path: ../openrig_core

flutter:
  uses-material-design: true
